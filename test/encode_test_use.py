import secure_python.secure_python as spy

#emd = encode main delete = this will delete the encoded file after encoding it, so only the encrypted version exists
#ems = encode main safe = this will not delte the encoded file after encoding it, so there will be the original version still there

spy.dme("test_file.py")